[pytest]
filterwarnings =
    ignore:Using .* with .*starlette.testclient.* is deprecated
    ignore:Field 'lifespan' has an incomplete definition:Warning
markers =
    smoke: requires a live Dameng database (set DM_SMOKE=1 and fill .env)
